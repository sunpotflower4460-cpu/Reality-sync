name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test-and-build:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22
          package-manager-cache: false

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Emit generated lockfile for bootstrap
        run: |
          echo '===REALITYSYNC_PACKAGE_LOCK_BEGIN==='
          cat package-lock.json
          echo '===REALITYSYNC_PACKAGE_LOCK_END==='

      - name: Run tests
        run: npm test

      - name: Build
        run: npm run build
